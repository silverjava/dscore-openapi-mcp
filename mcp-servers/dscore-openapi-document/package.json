{
  "name": "dscore-openapi-document-mcp",
  "version": "1.0.1",
  "description": "MCP server providing document listing and retrieval tools for DS Core Open API with AI LLM integration",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "dscore-openapi-document": "./dist/cli.js"
  },
  "scripts": {
    "build": "find src/ -name '*.ts' -exec npx tsc {} --outDir dist --target ES2020 --module commonjs --esModuleInterop true --skipLibCheck true \\; && node scripts/add-shebang.js",
    "dev": "nodemon --exec ts-node src/index.ts",
    "start": "node dist/index.js",
    "lint": "eslint src/**/*.ts",
    "clean": "rimraf dist",
    "test": "jest"
  },
  "keywords": [
    "mcp",
    "document",
    "files",
    "dscore",
    "openapi",
    "ai",
    "llm"
  ],
  "author": "GenAPI",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.0",
    "uuid": "^13.0.0",
    "yargs": "^17.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "rimraf": "^5.0.0"
  }
}